<!DOCTYPE html>
<html>
<head>
    <title>ItsolutionStuff.com</title>
</head>
<body>
    <h1>{{ $mailData['first'] }}</h1>
    <p>{{ $mailData['last'] }}</p>
    <p>{{ $mailData['email'] }}</p>
    <p>{{ $mailData['title'] }}</p>
  
    <p>{{ $mailData['body']}}</p>
     
    <p>Thank you</p>
</body>
</html>